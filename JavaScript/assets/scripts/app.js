/*declaring  and assigning variable*/
const defaultResult = 0;
let currentResult = defaultResult;
currentResult = currentResult + 105
let calculatioDescription = `${defaultResult} + 105`;
outputResult(currentResult, calculatioDescription);

function addNum(num1, num2){
    const result = num1 + num2;
    return result
}